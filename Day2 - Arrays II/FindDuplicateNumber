class Solution {
public:
    int findDuplicate(vector<int>& nums) 
    {
        sort(nums.begin(),nums.end());
        int s = 0,e = nums.size()-1;
        while(s<=e)
        {
            int mid = s+ (e-s)/2;
            if(nums[mid]==mid)
            e = mid-1;
            else if(nums[mid]!=mid)u e
            s = mid+1;
        } 
        return s;   
    }
};